/*
 * Compile the shared UART framing library through the existing Core source
 * entry while keeping its implementation outside CubeMX-managed directories.
 */
#include "../../../../shared/uart_framing/uart_framing.c"
